version: 2

models:
  - name: stg_customers
    description: "Dados normalizados e padronizados de clientes."

    columns:
      - name: customer_id
        tests:
          - not_null
      - name: customer_unique_id
        tests:
          - not_null